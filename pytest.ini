[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    network: tests requiring external network access (e.g. HuggingFace Hub downloads)
    slow: long-running tests (> 30s); deselect with -m "not slow"
addopts = -v --tb=short
testpaths = tests
