# DeepSeek-V3 operator screenshot tool dependencies
#
# torch: TorchDispatchMode + meta-tensor forward pass
torch>=2.0.0

# transformers: model config/modeling utilities
# Keep the upper bound on the latest version this fake-tracing stack is
# validated against; newer 5.x releases have changed enough internals to
# alter captured training graph structure.
# Tested on: 4.57.6 and 5.4.0
transformers>=4.36.0,<5.5.0

# openpyxl: write formatted Excel workbook
openpyxl>=3.1.0

# networkx: computation graph (DAG) construction and analysis
networkx>=3.0

# onnx: export computation graph for Netron visualization
onnx>=1.14.0

# pandas: training search result tables (training_search_util)
pandas>=2.0.0

# server: FastAPI service dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
