# DeepSeek-V3 operator screenshot tool dependencies
#
# torch: TorchDispatchMode + meta-tensor forward pass
torch>=2.0.0

# transformers: model config/modeling utilities
# No upper-version pin: runtime version shims in python/zrt/graph/compat.py
# handle API removals between 4.x and 5.x (e.g. is_flash_attn_greater_or_equal_2_10).
# Tested on: 4.57.6 and 5.4.0
transformers>=4.36.0

# openpyxl: write formatted Excel workbook
openpyxl>=3.1.0

# networkx: computation graph (DAG) construction and analysis
networkx>=3.0

# onnx: export computation graph for Netron visualization
onnx>=1.14.0
